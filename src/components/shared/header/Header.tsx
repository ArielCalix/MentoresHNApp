import styled from 'styled-components'

export const Header = styled.header`
  background: var(--light-shades);
  transition: all 0.5s;
  z-index: 997;
  padding: 15px 0;
  box-shadow: 0px 0 18px var(--main-brand-transparent);
`